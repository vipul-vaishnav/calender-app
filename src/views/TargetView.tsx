import React, { FC, ReactElement } from 'react'
import { ITargetView } from './interfaces/ITargetView'

const TargetView: FC<ITargetView> = (): ReactElement => {
  return (
    <div>TargetView</div>
  )
}

export default TargetView