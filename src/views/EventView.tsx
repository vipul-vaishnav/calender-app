import React, { FC, ReactElement } from 'react'
import { IEventView } from './interfaces/IEventView'

const EventView: FC<IEventView> = (): ReactElement => {
  return (
    <div>EventView</div>
  )
}

export default EventView